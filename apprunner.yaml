version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip3 install uv
      - uv venv /opt/venv
      - uv pip install -r requirements.txt

run:
  runtime-version: 3.11
  pre-run:
    - pip3 install uv
  command: /opt/venv/bin/python app.py
  network:
    port: 8080
    env: PORT
